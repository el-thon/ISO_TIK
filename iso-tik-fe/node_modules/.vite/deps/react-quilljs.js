import { i as __toCommonJS } from "./chunk-C3fVp-vY.js";
import { t as require_react } from "./react-BflXS9Hd.js";
import { r as quill_exports, t as init_quill } from "./quill-C-LC7D6k.js";

//#region node_modules/react-quilljs/esm/index.js
var import_react = require_react();
var theme = "snow";
var modules = {
	toolbar: [
		[
			"bold",
			"italic",
			"underline",
			"strike"
		],
		[{ align: [] }],
		[{ list: "ordered" }, { list: "bullet" }],
		[{ indent: "-1" }, { indent: "+1" }],
		[{ size: [
			"small",
			false,
			"large",
			"huge"
		] }],
		[{ header: [
			1,
			2,
			3,
			4,
			5,
			6,
			false
		] }],
		[
			"link",
			"image",
			"video"
		],
		[{ color: [] }, { background: [] }],
		["clean"]
	],
	clipboard: { matchVisual: false }
};
var formats = [
	"bold",
	"italic",
	"underline",
	"strike",
	"align",
	"list",
	"indent",
	"size",
	"header",
	"link",
	"image",
	"video",
	"color",
	"background"
];
function assign(target, _varArgs) {
	if (target === null || target === void 0) throw new TypeError("Cannot convert undefined or null to object");
	const to = Object(target);
	for (let index = 1; index < arguments.length; index++) {
		const nextSource = arguments[index];
		if (nextSource !== null && nextSource !== void 0) {
			for (const nextKey in nextSource) if (Object.prototype.hasOwnProperty.call(nextSource, nextKey)) to[nextKey] = nextSource[nextKey];
		}
	}
	return to;
}
/**
*
* @param options Quill static options. https://github.com/gtgalone/react-quilljs#options
* @returns Returns quill, quillRef, and Quill. https://github.com/gtgalone/react-quilljs#return
*/
const useQuill = (options = {
	theme,
	modules,
	formats
}) => {
	const quillRef = (0, import_react.useRef)(null);
	const [isLoaded, setIsLoaded] = (0, import_react.useState)(false);
	const [obj, setObj] = (0, import_react.useState)({
		Quill: void 0,
		quillRef,
		quill: void 0,
		editorRef: quillRef,
		editor: void 0
	});
	(0, import_react.useEffect)(() => {
		if (!obj.Quill) setObj((prev) => assign(prev, { Quill: (init_quill(), __toCommonJS(quill_exports)).default }));
		if (obj.Quill && !obj.quill && quillRef && quillRef.current && isLoaded) {
			const opts = assign(options, {
				modules: assign(modules, options.modules),
				formats: options.formats || formats,
				theme: options.theme || theme
			});
			const quill = new obj.Quill(quillRef.current, opts);
			setObj(assign(assign({}, obj), {
				quill,
				editor: quill
			}));
		}
		setIsLoaded(true);
	}, [
		isLoaded,
		obj,
		options
	]);
	return obj;
};

//#endregion
export { useQuill };
//# sourceMappingURL=react-quilljs.js.map